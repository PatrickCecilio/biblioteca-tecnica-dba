ambientes RAC ou dataguard, todos os nós (e bases standby) devem usar o mesmo valor de DB_FILES
1024 descritores de arquivo por processo por padrão
ulimit -n
definido o parâmetro MAXDATAFILES no CREATE DATABASE, que indica quantos datafiles o control file comportava inicialmente.


SELECT VALUE
FROM V$PARAMETER
WHERE NAME = 'db_files'; = DB_FILES é um parâmetro estático e, em alguns casos, só aparece quando foi explicitamente configurado no SPFILE/PFILE. Se não foi definido, o banco usa o default (200) para Oracle 19c.

================================
se você adicionar um datafile cujo número exceda MAXDATAFILES mas seja menor ou igual a DB_FILES, o control file expande-se automaticamente para acomodar
docs.oracle.com
. Portanto não é necessário recriar manualmente o control file (se o DB_FILES aumentar, basta reiniciar).
