sudo dnf -y update

sudo dnf install -y

sudo dnf -qy module disable postgresql

sudo dnf install -y postgresql16 postgresql16-server postgresql16-contrib

sudo /usr/pgsql-16/bin/postgresql-16-setup initdb

sudo systemctl enable --now postgresql-16

sudo systemctl status postgresql-16

psql --version

sudo -u postgres psql -c "ALTER ROLE postgres WITH PASSWORD '<PASSWORD>';"

gzip -t <BACKUP_ARCHIVE>.tar.gz

tar -tzf <BACKUP_ARCHIVE>.tar.gz | head -50

mkdir -p <RESTORE_WORKDIR>

tar -xzf <BACKUP_ARCHIVE>.tar.gz -C <RESTORE_WORKDIR>

pg_restore --list <RESTORE_WORKDIR>/<DUMP_FILE>.dump | head -40
